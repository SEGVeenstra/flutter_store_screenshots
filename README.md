# Flutter Store Screenshots

This package helps you create beautiful screenshots for stores like the App and Play Store, using the technique/framework you already know, FLUTTER!

Instead if having to learn a new tool, or setup complicated UI automation tests, you simply use what you've already have to build your screenshots.

Because this package uses your app's actual widgets, updating is as simple as simply regenerating the screenshots.